/*
1. C 语言的基本模块是什么？

C 语言的基本模块是函数 。一个 C 程序由一个或多个函数组成，其中 main 函数是程序执行的入口，其他函数可被 main 函数调用，实现代码的模块化、复用性。

2. 什么是语法错误？写出一个英语例子和 C 语言例子
语法错误：代码违反编程语言语法规则，编译器 / 解释器无法识别、编译执行的错误，类似自然语言里 “词序混乱、不符合语法规则的句子”。
英语例子：She go to school every day.（语法错误，主语 she 是第三人称单数，一般现在时动词应为 goes ）。
C 语言例子：printf("Hello, World!)（字符串末尾缺少右双引号，不符合 C 语言字符串语法 ）。

3. 什么是语义错误？写出一个英语例子和 C 语言例子
语义错误：代码语法合法，但逻辑、含义有问题，程序行为不符合预期，类似自然语言里 “语法对但表意荒谬、逻辑错误的句子”。
英语例子：The dog drove the car to park.（语法正确，但语义不合理，狗实际无法开车 ）。
C 语言例子：*/

/*
#include <stdio.h>
int num = 10;
// 意图可能是判断 num 是否大于 5，但写成赋值，逻辑错误
if (num = 5) { 
    printf("num 大于 5");
}
*/

#include <stdio.h>
int main(void) /* 该程序打印一年有多少周 */
{
    int s; 
    s = 56;
    printf("There are %d weeks in a year.", s);
    return 0; 
}

