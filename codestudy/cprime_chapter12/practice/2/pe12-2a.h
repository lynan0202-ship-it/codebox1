// ------------------------------
// 题干：定义头文件，声明模式设置、数据输入、油耗显示函数
// 知识要点：
//  1. 头文件保护宏（避免重复包含）
//  2. 函数声明（供其他文件调用）
// 总体逻辑：
//  1. 用#ifndef包裹头文件内容，防止重复定义
//  2. 声明三个核心函数：set_mode、get_info、show_info
#ifndef PE12_2A_H
#define PE12_2A_H

void set_mode(int mode);  // 设置计算模式（0=公制，1=美制）
void get_info(void);      // 根据模式输入距离和燃料
void show_info(void);     // 根据模式计算并显示油耗

#endif

// 测试验证方案：
//  1. 怎么运行：需和pe12-2a.c、pe12-2b.c一起编译（如 `gcc pe12-2b.c pe12-2a.c -o fuel`）
//  2. 预期结果：头文件正确连接，程序正常运行
//  3. 检查点：编译时无“未定义函数”错误
// 易错点提醒：
//  1. 忘记写头文件保护宏，导致重复定义报错
// 拓展思考：
//  1. 若要添加新函数，如何在头文件中扩展声明？