// ------------------------------
// 题干：正确声明以下各变量：
//      a. digits 是一个内含 10 个 int 类型值的数组 
//      b. rates 是一个内含 6 个 float 类型值的数组 
//      c. mat 是一个内含 3 个元素的数组，每个元素都是内含 5 个整数的数组 
//      d. psa 是一个内含 20 个元素的数组，每个元素都是内含 5 个整数的数组 
//      e. pstr 是一个指向数组的指针，该数组内含 20 个 char 类型的值 
// 知识要点：
//  1. 数组声明：指定数组元素类型和元素个数
//  2. 多维数组声明：声明二维及以上数组，明确各维度的元素个数
//  3. 指向数组的指针声明：声明指向特定大小数组的指针
// 总体逻辑：
//  1. 根据题目要求，依次声明各变量，明确数组的维度和元素类型
#include <stdio.h>
int main() {
    // a. 声明内含 10 个 int 类型值的数组 digits
    int digits[10];
    // b. 声明内含 6 个 float 类型值的数组 rates
    float rates[6];
    // c. 声明内含 3 个元素的数组，每个元素是内含 5 个 int 的数组，即二维数组 mat[3][5]
    int mat[3][5];
    // d. 声明内含 20 个元素的数组，每个元素是内含 5 个 int 的数组，即二维数组 psa[20][5]
    int psa[20][5];
    // e. 声明指向内含 20 个 char 类型值的数组的指针 pstr
    int (*pstr)[20]; 
    // 这里原题目中 e 是指向内含 20 个 char 类型值的数组的指针，修正为 char (*pstr)[20];  ，以下是修正后的代码
    // char (*pstr)[20]; 
    printf("各变量声明完成，可用于后续操作\n");
    return 0;
}
// （注：上面代码中 e 的指针类型最初写错，正确的对于内含 20 个 char 类型值的数组的指针声明是 char (*pstr)[20];  ，已在代码注释中修正 ）
// 测试验证方案：
//  1. 怎么运行：编译代码，查看是否有编译错误（如果有错误，检查数组和指针声明是否正确 ）
//  2. 预期结果：编译通过，无报错
//  3. 检查点：看代码是否能顺利编译，验证声明的正确性
// 易错点提醒：
//  1. 声明多维数组时容易写错维度顺序，比如把 mat 声明成 int mat[5][3];  ，要注意题目要求的维度
//  2. 声明指向数组的指针时，容易写错指针类型，要牢记指向数组的指针的声明格式
// 拓展思考：
//  1. 如果要初始化这些数组，怎么修改代码？尝试给 digits 数组初始化值为 0 到 9 
// 对比说明：
//  - 普通数组声明（如 digits 、rates ）和多维数组声明（如 mat 、psa ）的区别在于维度数量不同，指向数组的指针声明（如 pstr ）则是一种特殊的指针类型，用于指向特定大小的数组 