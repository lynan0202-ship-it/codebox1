// ------------------------------
// 题干：编写 `show1` 函数，参数为 `double ar1[]` 和 `int n`，输出数组元素个数
// 知识要点：
//  1. 一维数组的参数传递：数组名退化为指针，需显式传递元素个数。
//  2. 函数遍历数组：通过循环遍历数组，需依赖传入的 `n` 控制边界。
//  3. 函数定义与调用：简单函数的实现与调用流程。
// 总体逻辑：
//  1. 定义 `show1` 函数，接收数组和元素个数。
//  2. 遍历数组，输出元素个数（或元素值，题目隐含需求）。
#include <stdio.h>

// 函数原型：输出一维数组的元素个数（或值，这里演示输出个数）
void show1(double ar1[], int n);

int main() {
    double ar1[10] = {1.1, 2.2, 3.3}; // 示例数组
    int n = 10;

    // 调用 show1 函数
    show1(ar1, n);

    return 0;
}

// 函数定义：遍历数组，输出元素个数（或值，这里输出个数演示）
void show1(double ar1[], int n) {
    printf("数组元素个数：%d\n", n);
    // 若需输出元素值，可添加遍历逻辑：
    // for (int i = 0; i < n; i++) {
    //     printf("%.1f ", ar1[i]);
    // }
}

// 测试验证方案：
//  1. 怎么运行：编译运行（如 `gcc -o test test.c`，然后 `./test`）。
//  2. 预期结果：输出数组元素个数（如 `10`）。
//  3. 检查点：确认输出个数与 `n` 的值一致。
// 易错点提醒：
//  1. 容易忘记传入 `n`，导致数组越界（如循环条件写 `i < 10` 而非 `i < n`）。
//  2. 混淆数组名和指针，误以为 `ar1` 能直接获取元素个数。
// 拓展思考：
//  1. 如果数组是动态分配的（`double *ar1 = malloc(5 * sizeof(double))`），如何修改函数？
//  2. 如何让函数同时输出元素值和个数？
// 对比说明：
//  - 数组传参时，数组名退化为指针，丢失了元素个数信息，必须显式传递 `n`，这与其他语言（如 Python）不同。