// ------------------------------
// 题干：声明一个800×600的int类型数组。
// 知识要点：
//  1. 多维数组声明：指定各维度的元素个数，声明二维数组 
// 总体逻辑：
//  1. 按照二维数组声明语法，声明一个800行600列的int数组
#include <stdio.h>
int main() {
    // 声明800行600列的int类型二维数组
    int arr[800][600];
    printf("800×600的int类型数组声明完成\n");
    return 0;
}
// 测试验证方案：
//  1. 怎么运行：编译并运行代码，查看是否有编译错误
//  2. 预期结果：编译通过，输出“800×600的int类型数组声明完成”
//  3. 检查点：看代码是否能顺利编译，验证多维数组声明的正确性
// 易错点提醒：
//  1. 容易写错数组维度，比如写成int arr[600][800]; ，要注意题目要求的行列数
// 拓展思考：
//  1. 如果要初始化这个数组的所有元素为0，怎么修改代码？（利用循环初始化）
// 对比说明：
//  - 多维数组声明和普通数组声明类似，只是多了维度，这里的二维数组声明明确了两个维度的大小，用于存储二维数据 