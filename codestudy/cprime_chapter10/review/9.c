// ------------------------------
// 题干：内含10个元素的数组下标范围是什么？
// 知识要点：
//  1. 数组下标规则：C语言里数组下标从0开始计数 
//  2. 数组元素个数与下标关系：有n个元素的数组，下标范围是0到n - 1 
// 总体逻辑：
//  1. 根据数组下标从0开始的规则，计算10个元素数组的下标范围
#include <stdio.h>
int main() {
    // 对于内含10个元素的数组，下标从0开始，到9结束
    // 这里通过输出说明下标范围
    printf("内含10个元素的数组，下标范围是：0 到 9\n");
    return 0;
}
// 测试验证方案：
//  1. 怎么运行：编译并直接运行代码（如gcc -o test test.c ，然后./test ）
//  2. 预期结果：输出“内含10个元素的数组，下标范围是：0 到 9”
//  3. 检查点：看输出内容是否符合数组下标规则
// 易错点提醒：
//  1. 容易认为下标从1开始，要牢记C语言数组下标从0起始
// 拓展思考：
//  1. 如果数组是动态分配有10个元素（如int *arr = (int *)malloc(10 * sizeof(int)); ），下标范围还是这样吗？为什么？
// 对比说明：
//  - 不管数组是静态声明还是动态分配，只要元素个数是10，下标范围都是0到9，这是C语言数组下标规则决定的 