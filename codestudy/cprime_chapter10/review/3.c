// ------------------------------
// 题干：在复习题 1 中，ref 的地址是什么？ref + 1 是什么意思？++ref 指向什么？
// 知识要点：
//  1. 数组名的本质：数组名代表数组首元素的地址
//  2. 指针的算术运算：指针加减整数，是按照所指向数据类型的大小来移动的
//  3. 数组名的特性：数组名是一个常量指针，不能被修改（如不能进行 ++ref 操作 ）
// 总体逻辑：
//  1. 输出 ref 的地址（即数组首元素地址）
//  2. 解释 ref + 1 的含义（指向数组第二个元素的地址 ）
//  3. 说明 ++ref 是不合法的操作及原因
#include <stdio.h>
int main() {
    int ref[] = {8, 4, 0, 2};
    // 输出 ref 的地址，即数组首元素地址
    printf("ref 的地址是：%p\n", (void *)ref);
    // 输出 ref + 1 的地址，即数组第二个元素的地址
    printf("ref + 1 的地址是：%p\n", (void *)(ref + 1));
    // 尝试 ++ref 会报错，这里注释掉错误代码演示
    // ++ref; 
    printf("ref 是常量指针，不能进行 ++ref 操作，会导致编译错误\n");
    return 0;
}
// 测试验证方案：
//  1. 怎么运行：编译运行代码，观察输出和编译提示
//  2. 预期结果：输出 ref 和 ref + 1 的地址（两者差值通常为 4 ，因为 int 类型占 4 字节 ），同时提示 ref 不能 ++
//  3. 检查点：重点看地址差值以及编译对 ++ref 的报错提示
// 易错点提醒：
//  1. 容易误以为 ref 是普通变量可以进行 ++ 操作，要记住数组名是常量指针
// 拓展思考：
//  1. 如果是指针变量（如 int *p = ref; ），p + 1 和 ++p 分别是什么情况，有什么区别？
// 对比说明：
//  - 数组名 ref ：是常量指针，不能修改；指针变量 p ：是变量指针，可以进行 ++ 等操作。ref + 1 和 p + 1 都是指针移动一个元素大小，但 ref 不能自增，p 可以