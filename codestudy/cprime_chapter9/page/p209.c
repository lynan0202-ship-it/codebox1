/* 
知识要点总结：
1. 函数的定义：掌握如何定义无返回值（void 类型）、无参数以及后续可拓展有参数的函数，理解函数体是完成特定任务的代码块。
2. 函数的调用：学会在 main 函数中调用自定义函数，明白程序执行流程是从 main 开始，遇到函数调用就跳转执行对应函数，执行完再回到 main 继续。
3. 代码模块化思想：体会用函数把不同功能封装起来，让主程序逻辑更清晰，比如把打印星号栏的功能单独放一个函数里，主程序专注流程控制和功能组合。
4. 变量与循环：复习 for 循环的使用，以及在函数内部定义变量（如 starbar 里的 count）来辅助完成任务。

代码总体逻辑说明：
程序从 main 函数开始执行，先输出提示信息，然后调用 starbar 函数打印一行 40 个星号的分隔栏，接着可以继续拓展调用其他函数（比如后续可添加处理数字的函数等，这里先简单示范函数基础用法），最后输出程序结束提示。通过函数调用，将不同功能拆分，让代码结构更清晰，符合书中强调的用函数组织程序、模块化设计的思想。
*/

#include <stdio.h>

// 定义 starbar 函数，功能是在一行打印 40 个星号并换行
// void 表示函数执行完没有返回值给调用它的地方
// 括号里 void 表示调用该函数时不需要传入数据
void starbar(void) {
    int count;  // 定义一个变量 count，用来控制循环次数
    // for 循环：初始化 count 为 1，只要 count 小于等于 40 就执行循环体，每次循环后 count 加 1
    for (count = 1; count <= 40; count++) {
        putchar('*');  // 每次循环打印一个星号
    }
    putchar('\n');  // 40 个星号打印完后，打印换行符，让后续内容另起一行
}

// main 函数是程序的入口，程序从这里开始执行
int main(void) {
    printf("这是一个展示函数用法的程序示例~\n");
    // 调用 starbar 函数，执行打印星号栏的功能
    starbar();
    printf("程序中函数调用演示完毕，后续可继续拓展其他函数功能哟~\n");
    return 0;  // main 函数返回 0，表示程序正常结束
}

/* 
代码测试及验证建议方案：
1. 基础功能测试：
   - 编译并运行代码，观察输出是否有提示文字，以及一行整齐的 40 个星号和结束提示。如果有，说明 starbar 函数调用成功，基础功能正常。
   - 可以修改 starbar 函数里 for 循环的条件，比如把 count <= 40 改成 count <= 20，重新编译运行，看星号数量是否变成 20 个，验证函数内部逻辑可修改性和对功能的影响。
2. 拓展测试（为后续学更多函数做准备）：
   - 尝试在 main 函数里添加对其他自定义函数的调用（比如自己再写一个打印横线的函数，或者处理数字计算的函数），看能否正常执行，检验多函数协作和程序模块化拓展性。
   - 故意在函数定义或调用时写错，比如把 starbar 写成 starBar（大小写错），看编译会不会报错，理解函数名必须准确，这是调用函数的易错点。
3. 可读性与模块化验证：
   - 把代码拿给刚学 C 语言的同学看，让其说说程序执行流程和每个函数作用，检验注释是否清晰、代码模块化是否有助于理解。要是同学能快速看懂，说明代码可读性和模块化设计合理。 
*/