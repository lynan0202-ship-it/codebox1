/*
4. 测试及验证建议方案
编译运行步骤（以 GCC 为例 ）：
编译命令：
在终端进入代码所在目录，执行：
bash
gcc -c usehotel.c -o usehotel.o
gcc -c hotel.c -o hotel.o
gcc usehotel.o hotel.o -o hotel_app


解释：
gcc -c 源文件 -o 目标文件 ：把每个 .c 文件编译成目标文件（.o ），只编译不链接，检查语法错误。
gcc 目标文件1 目标文件2 -o 可执行文件 ：把多个目标文件链接成可执行程序 hotel_app 。
或者简化为一步编译（适合小项目 ）：
bash
gcc usehotel.c hotel.c -o hotel_app

运行程序：
执行 ./hotel_app（Linux/Mac ）或 hotel_app.exe（Windows ），启动程序测试。
具体测试用例：
基础功能验证：
选择酒店 1（编号 1 ），输入天数 3 ，检查费用计算是否正确：
第 1 天：180.00 * 1 = 180
第 2 天：180.00 * 0.95 = 171
第 3 天：180.00 * 0.95^2 = 162.45
总和：180 + 171 + 162.45 = 513.45 ，程序应输出 The total cost will be $513.45. 。
选择退出（编号 5 ），检查是否打印 Thank you and goodbye. 并结束程序。
输入校验测试：
在 menu 输入字母（如 abc ），检查是否提示 Enter an integer from 1 to 5, please. ，且能继续接收整数输入。
在 getnights 输入字母（如 xyz ），检查是否提示 Please enter an integer, such as 2. ，且能重新输入天数。
在 menu 输入 6（超出范围 ），检查是否提示重新输入，直到输入 1 - 5 的整数。
边界值测试：
入住天数 1 ：费用应为房价原价（无折扣 ），如选酒店 2（225.00 ），费用 225.00 。
入住天数 0 ：虽然 getnights 理论上不会让 nights=0（因为 scanf 校验整数，但可以手动改代码测试 ），看 showprice 如何处理（本示例代码中 nights 是用户输入的正整数，所以不用测，但要知道实际项目需考虑所有边界 ）。
多文件协作验证：
修改 hotel.h 中 HOTEL1 的房价（如改成 200.00 ），重新编译程序，测试选择酒店 1 时房价是否更新，验证头文件常量的 “一处修改，处处生效”。
在 hotel.c 中修改 showprice 的折扣逻辑（如把 DISCOUNT 改成 0.9 ），重新编译，测试费用计算是否变化，验证函数实现的修改效果。
性能与鲁棒性测试（简单示例可忽略复杂性能，主要看逻辑 ）：
连续多次选择酒店、输入天数，检查程序是否稳定，有无内存泄漏（小示例不会有明显泄漏，但大项目要关注 ）。
输入极大天数（如 100 ），检查费用计算是否正确（虽然数值大，但程序应能处理 ），看是否有溢出风险（double 类型能存较大数值，本示例没问题 ）。
通过这些测试，能验证多文件程序的协作逻辑、函数功能、输入校验、边界处理是否正确，帮助初学者掌握多文件编程、模块化开发的核心知识点 。
*/