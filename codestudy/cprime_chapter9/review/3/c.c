// ------------------------------
// 题干：which()接受两个可存储 double 类型变量的地址，返回一个 double 类型的地址
// 知识要点：
//  1. 指针作为函数参数和返回值的用法
//  2. 函数头中指针类型的书写，包括参数和返回值
// 总体逻辑：
//  1. 确定函数参数是两个 double 类型指针（地址），返回值是 double 类型指针
//  2. 写出对应的函数头，体现指针参数和指针返回值
#include <stdio.h>

// 函数头：返回值类型是 double *（指向 double 类型的指针，即地址）
// 参数：两个 double 类型指针 addr1 和 addr2
double *which(double *addr1, double *addr2); 

// main 函数简单占位
int main() {
    return 0;
}

// 测试验证方案：
//  1. 怎么运行：
//     - 保存代码为 which_head.c
//     - 用 gcc 编译检查语法：gcc -c which_head.c 
//  2. 预期结果：
//     - 编译无报错，函数头语法正确 
//  3. 检查点：
//     - 看参数和返回值是否都是 double 类型指针
// 易错点提醒：
//  1. 容易把指针类型写错，要记得是 double * 表示地址
//  2. 注意返回值也是指针类型，写法是 double *
// 拓展思考：
//  1. 函数返回指针时要注意什么？比如避免返回局部变量的地址
//  2. 这个函数可能的功能，比如比较两个 double 变量地址对应的值，返回较大值的地址，试着构思函数体 