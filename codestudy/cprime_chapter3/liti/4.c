//fathm_ft.c--把英寻转换为英尺
#include <stdio.h>
int main (void)
{
    int mate = 2;
    long shot = 53456;
    char grade = 'A';
    double log = 2.71828;
    printf("The odds against the %d were %ld to 1.\n A score of %lf is not an %c grade",mate,shot,log,grade); 
    return 0;
}