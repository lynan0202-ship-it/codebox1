/*
4. 第 4 题：计算表达式的值
要点：按照运算符优先级，逐步计算每个表达式的值，理解不同运算符（关系、逻辑、三目等 ）的运算顺序。
知识点：运算符优先级、关系运算、逻辑运算、三目运算。
*/
#include <stdio.h>

int main(void) {
    // a. 5 > 2 为真（值为1），3 > 2 为真（值为1），逻辑与运算，1 && 1结果为1
    int a = 5 > 2 && 3 > 2;
    // b. 3 + 4 > 2 为7>2即1，3 < 2为0，逻辑与运算，1 && 0结果为0
    int b = 3 + 4 > 2 && 3 < 2;
    // c. 假设x=5，y=3（可自行设定值，这里只是演示运算，实际x、y未定义时不能直接用，这里假设赋值）
    int x = 5, y = 3;
    int c = x >= y || y > x;
    // d. 6 > 2 为1，5 + 1结果为6
    int d = 5 + (6 > 2);
    // e. 'X'的ASCII码是88，'T'是84，88 > 84为1，所以取10
    int e = 'X' > 'T' ? 10 : 5;
    // f. 假设x=6，y=4（演示用，实际需合理赋值），x > y为1，y > x为0，1 > 0为1
    int f = x > y ? y > x : x > y;

    printf("a表达式值：%d\n", a);
    printf("b表达式值：%d\n", b);
    printf("c表达式值（x=5,y=3）：%d\n", c);
    printf("d表达式值：%d\n", d);
    printf("e表达式值：%d\n", e);
    printf("f表达式值（x=6,y=4）：%d\n", f);

    return 0;
}
// 这里要注意，对于涉及到未定义变量（如c、f中如果x、y没赋值的情况），实际代码中
// 需要合理赋值才能计算，这里只是按照题目需求演示运算过程，假设了x、y的取值来计算 

/*
a表达式值：1
b表达式值：0
c表达式值（x=5,y=3）：1
d表达式值：6
e表达式值：10
f表达式值（x=6,y=4）：0
*/