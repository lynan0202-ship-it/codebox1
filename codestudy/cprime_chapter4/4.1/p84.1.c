#include <stdio.h>

int main(void) 
{
    // 定义三组测试数据，模拟不同位数的数字
    int val1_1 = 12, val2_1 = 234, val3_1 = 1222;
    int val1_2 = 4, val2_2 = 5, val3_2 = 23;
    int val1_3 = 22334, val2_3 = 2322, val3_3 = 10001;

    // 场景 1：不指定固定字段宽度，输出可能参差不齐
    // 演示默认行为：按数字实际宽度输出，导致列不对齐
    printf("----- 场景 1：不指定固定字段宽度 -----\n");
    printf("%d %d %d\n", val1_1, val2_1, val3_1);
    printf("%d %d %d\n", val1_2, val2_2, val3_2);
    printf("%d %d %d\n", val1_3, val2_3, val3_3);
    printf("\n");

    // 场景 2：指定固定字段宽度（9 个字符宽度），让输出整齐对齐
    // %9d 表示用 9 个字符宽度输出整数，不足则补空格，确保列对齐
    printf("----- 场景 2：指定固定字段宽度（9 个字符） -----\n");
    printf("%9d %9d %9d\n", val1_1, val2_1, val3_1);
    printf("%9d %9d %9d\n", val1_2, val2_2, val3_2);
    printf("%9d %9d %9d\n", val1_3, val2_3, val3_3);
    printf("\n");

    // 场景 3：在转换说明间插入空白字符，避免数字溢出后紧连
    // %9d  %9d  %9d 中，两个 %9d 间的空格是普通字符，会被原样输出
    // 确保即使数字溢出字段宽度，下一个数字也不会紧挨着，保持可读性
    printf("----- 场景 3：转换说明间插入空白字符 -----\n");
    printf("%9d  %9d  %9d\n", val1_1, val2_1, val3_1);
    printf("%9d  %9d  %9d\n", val1_2, val2_2, val3_2);
    printf("%9d  %9d  %9d\n", val1_3, val2_3, val3_3);
    printf("\n");

    // 场景 4：在文字中嵌入数字，灵活指定字段宽度
    double distance = 10.22;
    // 场景 4-1：指定较小字段宽度（.2f 表示保留 2 位小数，整体宽度适配数字）
    // 让输出文字和数字紧凑结合，没有多余空白
    printf("----- 场景 4：文字中嵌入数字（适配宽度） -----\n");
    printf("Count Beppo ran %.2f miles in 3 hours.\n", distance);

    // 场景 4-2：指定较大字段宽度（%10.2f 表示用 10 个字符宽度输出浮点数）
    // 会补空格，让数字在文字中占固定位置，适合排版需求
    printf("Count Beppo ran %10.2f miles in 3 hours.\n", distance);

    return 0;
}

/*
----- 场景 1：不指定固定字段宽度 -----
12 234 1222
4 5 23
22334 2322 10001

----- 场景 2：指定固定字段宽度（9 个字符） -----
       12       234      1222
        4         5        23
    22334      2322     10001

----- 场景 3：转换说明间插入空白字符 -----
       12        234       1222
        4          5         23
    22334       2322      10001

----- 场景 4：文字中嵌入数字（适配宽度） -----
Count Beppo ran 10.22 miles in 3 hours.
Count Beppo ran      10.22 miles in 3 hours.
*/