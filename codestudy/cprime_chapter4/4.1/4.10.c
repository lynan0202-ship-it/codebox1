#include <stdio.h>
// 定义字符串常量 BLURB，后续用于格式化输出演示
#define BLURB "Authentic imitation!"

int main(void)
{
    // 1. %2s 格式说明
    //    - s 表示输出字符串类型
    //    - 2 是指定的最小字段宽度
    //    - 规则：如果字符串实际长度大于指定宽度，字段宽度会自动扩展为字符串实际长度，保证完整显示字符串
    //    - 这里 BLURB 字符串实际长度大于 2，所以按实际完整输出 "Authentic imitation!"
    printf("[%2s]\n", BLURB);

    // 2. %24s 格式说明
    //    - 24 是指定的最小字段宽度
    //    - 规则：如果字符串实际长度小于指定宽度，会在字符串左侧用空格填充，使整个字段宽度达到 24
    //    - BLURB 实际长度小于 24，所以在字符串左侧填充空格，让其在 24 字符宽度的字段中右对齐显示
    printf("[%24s]\n", BLURB);

    // 3. %24.5s 格式说明
    //    - 24 是最小字段宽度，.5 表示从字符串开头截取 5 个字符
    //    - 规则：先截取字符串的前 5 个字符，然后将截取后的内容放在 24 字符宽度的字段中，左侧用空格填充，实现右对齐
    //    - 先截取 "Authentic imitation!" 的前 5 个字符 "Authe"，再在其左侧填充空格，使整体占 24 字符宽度
    printf("[%24.5s]\n", BLURB);

    // 4. %-24.5s 格式说明
    //    - - 表示左对齐，24 是最小字段宽度，.5 表示截取前 5 个字符
    //    - 规则：先截取字符串的前 5 个字符，然后将截取后的内容放在 24 字符宽度的字段中，右侧用空格填充，实现左对齐
    //    - 先截取 "Authentic imitation!" 的前 5 个字符 "Authe"，再在其右侧填充空格，使整体占 24 字符宽度
    printf("[%-24.5s]\n", BLURB);

    return 0;
}
/*
[Authentic imitation!]
[    Authentic imitation!]
[                   Authe]
[Authe                   ]
*/