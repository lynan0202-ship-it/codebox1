/* 
问题4题干：给定复习题3中的程序和文件，下面哪一条是有效的命令？
a. essayct < essay 
b. count essay 
c. essay > count 

知识要点：
1. 命令行基本结构：程序名 [参数] [重定向]
2. 程序输入来源：
   - 标准输入（通过<重定向）
   - 命令行参数（需程序主动处理argv）

总体逻辑：
通过注释详细分析每条命令的合法性，无需编译代码

代码功能：
无实际代码，通过注释解释命令行语法
*/

/* 
命令行分析：
有效命令：count < essay > essayct （对应复习题3的正确用法）

逐条解释：
a. essayct < essay 
   - 错误：essayct不是可执行程序，无法处理输入重定向

b. count essay 
   - 错误：count程序设计为从标准输入读取数据，不处理命令行参数
   - 若需处理文件名，程序应使用argv主动打开文件（后续章节内容）

c. essay > count 
   - 错误：essay不是可执行程序，无法将输出写入count文件

正确用法演示：
编译count程序后，执行：
./count < essay > essayct 
*/

/* 
测试验证方案：
1. 尝试错误命令：
   - 执行essayct < essay，观察"命令未找到"错误
   - 执行count essay，观察程序等待标准输入（因未触发文件重定向）
   - 执行essay > count，观察"essay不是命令"错误

2. 验证正确命令：
   - 按复习题3的测试方案执行有效命令，确认结果正确
*/