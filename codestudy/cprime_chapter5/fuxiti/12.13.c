/*第 12 题
a 部分：将变量 x 的值增加 10
可以用 x += 10; ，等价于 x = x + 10; ，简洁地实现变量 x 增加 10 的操作。
b 部分：将变量 x 的值增加 1
使用 x++; （后置自增，先使用 x 原来的值再自增 ）或者 ++x; （前置自增，先自增再使用 ），也可以写 x = x + 1; ，常用的简洁写法是 x++; 。
c 部分：将 a 与 b 之和的两倍赋给 c
代码为 c = 2 * (a + b); ，先计算 a 和 b 的和，再乘以 2 ，最后把结果赋值给 c 。
d 部分：将 a 与 b 的两倍之和赋给 c
代码是 c = a + 2 * b; ，先计算 b 的两倍，再和 a 相加，结果赋值给 c 。
第 13 题
a 部分：将变量 x 的值减少 1
可以用 x--; （后置自减 ）、--x; （前置自减 ）或者 x = x - 1; ，简洁写法常用 x--; 。
b 部分：将 n 除以 k 的余数赋给 m
在 C 语言中，使用取余运算符 % ，代码为 m = n % k; ，计算 n 除以 k 后的余数并赋值给 m 。
c 部分：q 除以 b - a 的差，并将结果赋给 p
代码是 p = q / (b - a); ，先计算 b - a 的差，再用 q 除以这个差，把结果赋值给 p ，要注意 b - a 不能为 0 ，否则会出现除 0 错误。
d 部分：a 与 b 之和除以 c 与 d 的乘积，并将结果赋给 x
代码为 x = (a + b) / (c * d); ，先分别计算分子 a + b 和分母 c * d ，然后做除法运算，结果赋值给 x ，同样要注意分母 c * d 不能为 0 ，避免除 0 错误。
这些题目主要围绕 C 语言的运算符、循环结构以及表达式运算等基础知识点，多练习有助于熟悉语法和逻辑～
*/